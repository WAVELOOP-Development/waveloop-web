/** @type {import('next-sitemap').IConfig} */
module.exports = {
  siteUrl: "https://waveloop.dev", // ✅ Your live domain
  generateRobotsTxt: true, // ✅ Automatically generate robots.txt
  sitemapSize: 7000, // Optional: limits number of URLs per sitemap file
};
